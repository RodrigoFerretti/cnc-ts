{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "run server",
            "command": "yarn ts-node-dev --respawn -r dotenv/config ./src/index.ts",
            "request": "launch",
            "type": "node-terminal"
        },
        {
            "name": "run and debug current .ts file",
            "command": "yarn ts-node-dev --respawn -r dotenv/config ${relativeFile}",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}
